import React from 'react';

export default function CartCheckoutBtn() {
    return (
        <button className="w-full mt-8 py-4 bg-black text-white text-xs font-bold rounded-md tracking-[0.2em] uppercase hover:bg-gray-800 transition-all">
            Proceed to Checkout
        </button>
    );
}
