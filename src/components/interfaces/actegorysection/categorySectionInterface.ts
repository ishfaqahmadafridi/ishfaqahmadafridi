export interface Category {
    id: number;
    title: string;
    page: string;
}

export interface CategorySectionProps {
    // Component doesn't require props currently
    // Add props here if needed in the future
}
